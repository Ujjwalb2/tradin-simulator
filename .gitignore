# Python
.venv/
__pycache__/
*.pyc
.DS_Store

# Research and build data: large (the 1m research file alone is 225 MB) and re-downloadable.
# Rebuild it with src/fetch_data.py, src/build_tf.py and src/build_nse.py (see README).
# replay/data/ is committed on purpose: it is what the hosted dashboard loads.
/data/
